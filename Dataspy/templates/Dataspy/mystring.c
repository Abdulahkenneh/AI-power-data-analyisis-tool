#include <stdio.h>

    int main(void){
       char character,str[strlen("There are two things")];
       str="There are two things";
       printf("%c , %s \n",character,str);
    
    
        return 0;
    }